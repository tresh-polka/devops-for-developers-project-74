localhost {
  log {
    format json
  }

  # Компрессия
  encode zstd

  # Самоподписной сертификат (для localhost)
  tls internal

  # Проксирование всех запросов в сервис app на порт 8080
  reverse_proxy /* app:8080
}